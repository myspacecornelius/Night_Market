import type { Config } from 'tailwindcss'
import tailwindcssAnimate from 'tailwindcss-animate'

export default {
  darkMode: ['class'],
  content: ['./index.html', './src/**/*.{ts,tsx}'],
  theme: {
    container: {
      center: true,
      padding: '1rem',
      screens: {
        '2xl': '1280px',
      },
    },
    extend: {
      fontFamily: {
        // Grotesk for headlines (assertive)
        grotesk: ['Inter', 'ui-sans-serif', 'system-ui', '-apple-system', 'sans-serif'],
        // Humanist for body (legible)  
        humanist: ['ui-sans-serif', 'system-ui', '-apple-system', 'Segoe UI', 'Roboto', 'sans-serif'],
        sans: ['ui-sans-serif', 'system-ui', '-apple-system', 'Inter', 'Segoe UI', 'Roboto', 'sans-serif'],
      },
      colors: {
        // Night Market Modern Palette
        ink: '#0B0B0C',
        bone: '#F6F6F6', 
        heat: '#E94A3D',
        olive: '#4E5A3A',
        steel: '#717784',
        neon: '#C3FFD0',
        
        // Semantic colors built on the palette
        background: 'hsl(var(--background))',
        foreground: 'hsl(var(--foreground))',
        primary: {
          DEFAULT: 'hsl(var(--primary))',
          foreground: 'hsl(var(--primary-foreground))',
        },
        secondary: {
          DEFAULT: 'hsl(var(--secondary))',
          foreground: 'hsl(var(--secondary-foreground))',
        },
        muted: {
          DEFAULT: 'hsl(var(--muted))',
          foreground: 'hsl(var(--muted-foreground))',
        },
        accent: {
          DEFAULT: 'hsl(var(--accent))',
          foreground: 'hsl(var(--accent-foreground))',
        },
        destructive: {
          DEFAULT: 'hsl(var(--destructive))',
          foreground: 'hsl(var(--destructive-foreground))',
        },
        border: 'hsl(var(--border))',
        input: 'hsl(var(--input))',
        ring: 'hsl(var(--ring))',
      },
      borderRadius: {
        lg: `var(--radius)`,
        md: `calc(var(--radius) - 2px)`,
        sm: 'calc(var(--radius) - 4px)',
      },
      boxShadow: {
        soft: '0 8px 30px rgba(0,0,0,.06)',
        glass: '0 8px 32px rgba(0,0,0,.08)',
        heat: '0 0 20px rgba(233, 74, 61, 0.3)',
        neon: '0 0 20px rgba(195, 255, 208, 0.4)',
      },
      backdropBlur: {
        xs: '2px',
      },
      animation: {
        'fade-in': 'fadeIn 0.2s ease-out',
        'slide-up': 'slideUp 0.2s ease-out',
        'pulse-heat': 'pulseHeat 2s ease-in-out infinite',
        'glow': 'glow 2s ease-in-out infinite alternate',
      },
      keyframes: {
        fadeIn: {
          '0%': { opacity: '0' },
          '100%': { opacity: '1' },
        },
        slideUp: {
          '0%': { transform: 'translateY(10px)', opacity: '0' },
          '100%': { transform: 'translateY(0)', opacity: '1' },
        },
        pulseHeat: {
          '0%, 100%': { boxShadow: '0 0 20px rgba(233, 74, 61, 0.3)' },
          '50%': { boxShadow: '0 0 30px rgba(233, 74, 61, 0.6)' },
        },
        glow: {
          '0%': { boxShadow: '0 0 20px rgba(195, 255, 208, 0.4)' },
          '100%': { boxShadow: '0 0 30px rgba(195, 255, 208, 0.8)' },
        },
      },
      transitionDuration: {
        '150': '150ms',
        '220': '220ms',
      },
      transitionTimingFunction: {
        'spring': 'cubic-bezier(0.68, -0.55, 0.265, 1.55)',
      },
    },
  },
  plugins: [tailwindcssAnimate],
} satisfies Config
